import { GameFrame } from "@/components/game-frame";

export const metadata = {
  title: "Dynamite",
};

export default function DynamitePage() {
  return <GameFrame src="/games/dynamite/index.html" title="Dynamite" />;
}
